version: "3"

services:
  postgres:
    image: "postgres:11.5"
    environment:
      - "POSTGRES_USER=test"
      - "POSTGRES_DB=test"
      - "POSTGRES_PASSWORD=test"
    expose:
      - "5433"
    ports:
      - "5433:5433"